// Minimal root build.gradle for CPM-FrameEditor multi-project setup

plugins {
    id 'eclipse'
}

subprojects {
    apply plugin: 'eclipse'

    tasks.withType(JavaCompile).configureEach {
        options.encoding = 'UTF-8'
    }
}
